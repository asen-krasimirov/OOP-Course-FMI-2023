#pragma once

const int MAX_VALUE_VAL = 20;
const int MAX_ROW_COUNT = 50;
const int MAX_COLUMN_COUNT = 10;

const int MAX_SIZE_FILE_NAME = 1024;

const int MAX_BUFFER_SIZE = 1024;

const int MAX_COMMAND_LINE_SIZE = 1024;
const int MAX_COMMAND_SIZE = 15;

enum class Alignment {
    left,
    right,
    center
};
